= content_for :header_img
  .header-img#evergreen-forest
    #content
      .col-lg-6.center: #current-jackpot
        .stat
          .label Current Jackpot:
          .amount $5,302,108
        .stat
          .label Interest Rate:
          .amount .90%

      .col-lg-6.center
        - if logged_in?
          = render 'transactions/deposit'
        - else
          = render 'users/signup'


h3 1) Deposit

h3 2) Earn interest

h3 3) Win the jackpot!